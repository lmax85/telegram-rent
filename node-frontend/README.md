# node-frontend

This is project using [nuxt.js](https://nuxtjs.org/) to create a frontend.

## Getting Started

You must have actual values for the environment:
```
BACKEND_URL=
APP_URL=
```
Just do it command `cp .env.example .env` from `node-fronted` folder.
